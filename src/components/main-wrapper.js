import React, { Component } from 'react';
import Nav from './nav';

class MainWrapper extends Component {
  render() {
    return (
      <React.Fragment>
        <Nav />
      </React.Fragment>
    );
  }
}

export default MainWrapper;
