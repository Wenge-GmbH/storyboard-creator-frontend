import React, { Component } from 'react';
import Nav from './nav';

class MainWrapper extends Component {
  render() {
    return (
      <div>
        <Nav />
      </div>
    );
  }
}

export default MainWrapper;
